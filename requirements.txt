# Robust SGD Benchmarking - Python dependencies
# Install: pip install -r requirements.txt
# Tested with Python 3.9–3.11 and PyTorch 2.x (MPS supported on PyTorch 2.0+).

torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21.0
matplotlib>=3.5.0
pandas>=1.3.0
Pillow>=9.0.0
tqdm>=4.62.0
